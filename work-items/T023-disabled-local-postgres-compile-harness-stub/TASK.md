# T023: Disabled Local Postgres Compile Harness Stub

Goal: Create a disabled-by-default local Postgres compile harness stub that proves the harness is disabled unless an explicit future gate enables it.
