# T013 Decisions

- T013 introduces a backend adapter contract and local JSON store prototype to simulate future backend ingestion without adding a real backend, database, migrations, scheduler, SARIF, GitHub Actions scanner mode, PR annotations, real customer ingestion, or external fetching.
- All storage is performed via local JSON files.
