# Decisions - T020C

- **Store Validation**: Added `scripts/validate-local-json-store.mjs` and `generate-local-json-store-sample.mjs` to ensure the local JSON store contract is validated in the CI/offline flow.
- **Offline Alignment**: Integrated new store scripts into `check:all-offline`.
