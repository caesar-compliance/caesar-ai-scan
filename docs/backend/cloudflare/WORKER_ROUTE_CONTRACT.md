# Worker Route Contract

Defines the route specifications for the Cloudflare Worker.

## Supported Routes
- GET /health
- GET /dashboard-summary
- GET /scan-runs
- GET /findings
- GET /inventory-components
- GET /review-items
- GET /import-ledger
- GET /manifest
